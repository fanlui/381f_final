<!DOCTYPE html>
<html>
<head>
    <% include ../partials/header.ejs %>
</head>
<body>
<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <h1>Register</h1>
            <%if (typeof msg !== 'undefined') { %>
            <div class="alert alert-<%=msg.type%>" role="alert"><%=msg.text%></div>
            <% } %>
            <form class="form-width" action="/register" method="post">
                <div class="form-group">
                    <label for="useridInput">Userid</label>
                    <input required="required" type="text" class="form-control" id="useridInput" name="userid" placeholder="Userid">
                </div>
                <div class="form-group">
                    <label for="passwordInput">Password</label>
                    <input required="required" type="password" class="form-control" id="passwordInput" name="password"
                           placeholder="Password">
                </div>
                <button type="submit" class="btn btn-primary">Register</button>
                <a href="/login" class="btn btn-link">Cancel</a>
            </form>
        </div>
    </div>
</div>

</body>
</html>
